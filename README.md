# ZP Labels

Dense ZPL label builder with template files, field editing, preview tools, and print fixes.

## Run

```bash
python3 -m http.server 8080
```

Hard-refresh GitHub Pages after deploy if the site looks cached.
